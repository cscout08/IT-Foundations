using System;


namespace Exercise_Array
{
    class Program
    {
        static void Main()



        {

            // set-up an array
            int[] myarray= new int[] { 0, 2, 5, 100, -1, 4, 8, -5 };

            // delcare a min and max

            int Min;
            int Max;

            // assume the first value in the
            // array is the Min and Max value
            // then work from there

            Max = myarray[0]; // I picked on the first number in the group for Max
            Min = myarray[0]; // I picked on the first number in the group for Min

            // need to create a loopoing method to go through the Min and Max

            foreach (int x in myarray)

                // fin the Min

                if (x < Min) // is less than Min)

                {
                    Min = x

                };
                        
                        // find the Max

                    if (// x is greater than Man)

            
   
          















        }
    }
}
